name: MiMoVoice
bundleId: com.mimo.voice
platforms: [iOS]
deploymentTarget: "16.0"
settings:
  SWIFT_VERSION: "5.9"
  TARGETED_DEVICE_FAMILY: "1,2"
  PRODUCT_BUNDLE_IDENTIFIER: com.mimo.voice
  CODE_SIGNING_ALLOWED: NO
  DEVELOPMENT_TEAM: ""
  IPHONEOS_DEPLOYMENT_TARGET: "16.0"
  GENERATE_INFOPLIST_FILE: NO
targets:
  MiMoVoice:
    type: application
    platform: iOS
    sources:
      - MimoVoice
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.mimo.voice
        CODE_SIGNING_ALLOWED: NO
        IPHONEOS_DEPLOYMENT_TARGET: "16.0"
        INFOPLIST_FILE: MimoVoice/Info.plist
        GENERATE_INFOPLIST_FILE: NO
    info:
      path: MimoVoice/Info.plist
